models:
  - name: default.table_pinval_assessment_card
    description: '{{ doc("table_pinval_assessment_card") }}'
    data_tests:
      - unique_combination_of_columns:
          name: default_pinval_assessment_card_unique_run_id_pin_card
          combination_of_columns:
            - run_id
            - meta_pin
            - meta_card_num
          allowed_duplicates: 0
          config:
            error_if: ">0"