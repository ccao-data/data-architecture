models:
  - name: default.pinval_comp
    description: '{{ doc("pinval_comp") }}'
    data_tests:
      - unique_combination_of_columns:
          name: default_pinval_comp_unique_run_id_pin_card_comp_num
          combination_of_columns:
            - run_id
            - pin
            - card
            - comp_num
          allowed_duplicates: 0
          config:
            error_if: ">1"